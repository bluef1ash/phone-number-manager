# 开发环境配置文件
debug: true

spring:
    config:
        import: optional:file:.env[.yml]

management:
    endpoints:
        enabled-by-default: true
        web:
            exposure:
                include: "*"

    endpoint:
        health:
            show-details: always
