# 开发环境配置文件
debug: true

spring:
    config:
        import: file:backend/.env[.yml]

management:
    endpoints:
        enabled-by-default: true
        web:
            exposure:
                include: "*"

    endpoint:
        health:
            show-details: always
