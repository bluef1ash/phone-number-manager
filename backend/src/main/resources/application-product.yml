spring:
    thymeleaf:
        cache: true

debug: false

custom:
    debug: false

management:
    endpoints:
        enabled-by-default: false

    endpoint:
        health:
            enabled: true
